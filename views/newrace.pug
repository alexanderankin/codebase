extends interface

block content
  h3
    a(href='/races') Races

  p.
    Enter race information:

  p You can add candidates on the edit screen.

  script.
    $(function() { $("#select-office").select2({}); });

  //- pre= JSON.stringify(offices, null, 2)

  <form method="post">
    <div class="form-group row">
      <label for="input-time" class="col-sm-2 form-control-label">Time</label>
      <div class="col-sm-10">
        <input type="date" class="form-control" id="input-time" name="time" style="max-width: 50%;">
      </div>
    </div>
    <div class="form-group row">
      <label class="col-sm-2 form-control-label">Office</label>
      <div class="col-sm-10">
        if (offices && offices.length)
          <select id="select-office" name="office" class="c-select">
            each office in offices
              <option value="#{office.id}">#{office.text}</option>
          </select>
        else
          p No offices yet!
      </div>
    </div>
    <div class="form-group row">
      <div class="col-sm-offset-2 col-sm-10">
        <button type="submit" class="btn btn-secondary">Save</button>
      </div>
    </div>
  </form>
